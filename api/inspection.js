const {appendValues}=require('../lib/sheets');
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({success:false,message:'Method Not Allowed'});try{const d=req.body||{};if(!d.plate)return res.status(400).json({success:false,message:'กรุณาระบุเลขทะเบียน'});if(!d.inspector)return res.status(400).json({success:false,message:'กรุณาระบุชื่อผู้ตรวจ'});const c=d.checklist||{},defectCount=Object.values(c).filter(v=>v==='เสีย').length,overall=defectCount?'พบข้อบกพร่อง':'ปกติ';await appendValues('Inspections!A:K',[new Date().toISOString(),d.date||'',d.plate||'',d.type||'',d.department||'',d.model||'',d.inspector||'',overall,defectCount,d.notes||'',JSON.stringify(c)]);res.status(200).json({success:true,overall,defectCount});}catch(e){res.status(500).json({success:false,message:e.message});}}
