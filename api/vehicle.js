const {getValues}=require('../lib/sheets');
export default async function handler(req,res){try{const plate=String(req.query.plate||'').trim();if(!plate)return res.status(400).json({success:false,message:'กรุณาระบุเลขทะเบียน'});const rows=await getValues('Vehicles!A:E');if(!rows.length)return res.status(500).json({success:false,message:'ชีต Vehicles ยังไม่มีข้อมูล'});const h=rows[0],idx=Object.fromEntries(h.map((x,i)=>[String(x).trim(),i]));for(const row of rows.slice(1)){if(String(row[idx['เลขทะเบียน']]??'').trim()===plate){if(idx['ใช้งาน']!==undefined&&String(row[idx['ใช้งาน']]).toUpperCase()==='FALSE')return res.status(404).json({success:false,message:'รถคันนี้ถูกปิดการใช้งาน'});return res.status(200).json({success:true,vehicle:{plate:row[idx['เลขทะเบียน']]||'',type:row[idx['ประเภทรถ']]||'',department:row[idx['หน่วยงาน']]||'',model:row[idx['ยี่ห้อ/รุ่น']]||''}})}}return res.status(404).json({success:false,message:'ไม่พบทะเบียน '+plate});}catch(e){return res.status(500).json({success:false,message:e.message});}}
